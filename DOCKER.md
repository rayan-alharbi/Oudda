# 🐳 دليل تشغيل "عُـدّة" عبر Docker

يوفر هذا الدليل تعليمات مفصلة حول كيفية تشغيل مشروع **عُـدّة (Oudda)** باستخدام تقنيات الحاويات (Docker & Docker Compose).

---

## 🚀 البدء السريع (Docker Hub)

إذا كنت ترغب في تشغيل الموقع مباشرة دون تحميل الكود المصدري، يمكنك سحب الصورة الجاهزة من Docker Hub:

```bash
# 1. سحب الصورة
docker pull rayanalharbi/oudda:V1

# 2. تشغيل الحاوية
docker run -d \
  --name oudda_app \
  -p 7433:7433 \
  --restart always \
  rayanalharbi/oudda:V1
```

سيكون الموقع متاحاً فوراً على: [http://localhost:7433](http://localhost:7433)

---

## 🛠️ التشغيل للمطورين (Local Build)

إذا قمت باستنساخ المستودع (Clone) وترغب في بناء الصورة بنفسك:

### 1. باستخدام Docker Compose (الموصى به)
يعد Docker Compose الطريقة الأسهل لإدارة الحاوية:

```bash
# بناء وتشغيل في الخلفية
docker compose up -d --build
```

### 2. البناء اليدوي (Manual Build)
```bash
# بناء الصورة
docker build -t oudda:local .

# التشغيل
docker run -d -p 7433:7433 oudda:local
```

---

## ⚙️ المواصفات التقنية للصورة

| الخاصية | القيمة |
| :--- | :--- |
| **قاعدة التشغيل** | Nginx Stable Alpine |
| **منفذ الاستماع الداخلي** | `7433` |
| **حجم الصورة التقريبي** | ~65MB |
| **نظام البناء** | Multi-stage build (Node 20 -> Nginx) |

---

## 🔍 الأوامر الشائعة

- **عرض السجلات (Logs):**
  ```bash
  docker logs -f oudda_app
  ```

- **إيقاف الحاوية:**
  ```bash
  docker compose stop
  ```

- **إزالة الحاوية والشبكة:**
  ```bash
  docker compose down
  ```

---

## 📄 ملاحظات
- تم ضبط Nginx داخل الحاوية ليعمل على المنفذ `7433`.
- يتم ضغط الملفات الثابتة وتجهيزها للإنتاج تلقائياً أثناء عملية البناء.

صُنع بـ ❤️ لمجتمع المطورين.
